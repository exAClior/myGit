#include <cstdio>
#include <algorithm>
#include <vector>
using namespace std;

vector<int> first, second;

int main(){
  int n;
  scanf("%d\n",&n);
  for(int i = 0; i < n ; i++){
    int tmp;
    scanf("%d",)
  }
  return 0;
}
